# Core RAG + graph + API. LLM + embeddings served by local Ollama (no SDK dep).
qdrant-client
networkx
fastapi
uvicorn
pydantic
# Semantic layer: OWL/SKOS ontology, OWL 2 RL reasoning, SHACL governance.
rdflib
owlrl
pyshacl
# Fine-tuning (optional, GPU): see finetune/README.md
# unsloth
